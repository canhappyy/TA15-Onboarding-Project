project_name = "ta15-onboarding"
environment  = "dev"
aws_region   = "ap-southeast-4"

# Terraform builds and pushes content-addressed Lambda images during apply.
build_lambda_images         = true
ingestion_schedules_enabled = false

frontend_origins = [
  "http://localhost:3000"
]
